# Conv 파라미터 수와 연산량(MACs) 구분

> Type: Concept

## 상황/배경(Context)
Conv 레이어의 파라미터 수를 셀 때 출력 feature map의 공간 크기(예: `30×30`)까지 곱해야 하는지 헷갈렸다. 결론은 “파라미터 수”와 “연산량(MACs/FLOPs)”을 구분하면 한 번에 정리된다.

---

## 정의(Definition)
- **파라미터 수(#params)**: 학습되는 가중치 `W`(그리고 선택적으로 bias)의 개수.
- **연산량(MACs/FLOPs 근사)**: forward 1회에서 그 가중치들이 **몇 번 사용되는지(=곱셈·덧셈 횟수)**.

## 핵심 아이디어(Key Ideas)
- Conv의 가중치는 공간 전체에 **공유(shared)** 된다. 그래서 **공간 크기(H×W)는 파라미터 수에 들어가지 않는다.**
- 대신 공간 크기(H×W)는 “같은 가중치를 몇 번 적용하느냐”이므로 **연산량에 들어간다.**

## 예시(Examples)
입력: `30×30×200`  
커널: `5×5`, 입력 채널 `200`  
필터 개수: `F` (예: 16개 또는 64개)

### 1) 파라미터 수
필터 1개가 가지는 가중치 개수:
- `5×5×200 = 5,000`

필터가 `F`개면:
- `#params = 5×5×200×F`
- (bias를 쓰면) `+ F`

예:
- `F=16` → `5×5×200×16 = 80,000` (bias 포함 시 `80,016`)
- `F=64` → `5×5×200×64 = 320,000` (bias 포함 시 `320,064`)

### 2) 연산량(MACs) — 왜 `30×30`이 여기서 등장하나
출력의 공간 크기가 `30×30`이고(예: stride=1, padding="same" 가정),
한 위치에서 한 필터가 하는 MACs가 대략 `5×5×200`이면,

- `MACs ≈ 30×30×5×5×200×F`

즉 `30×30`을 곱하는 순간, 그건 파라미터가 아니라 “계산량”을 세는 식이 된다.

## 주의할 점(Caveats)
- 출력 공간 크기 `H_out×W_out`은 stride/padding에 따라 달라진다. (위 연산량 식의 `30×30`은 “출력이 30×30일 때”의 값)
- bias는 파라미터 수에만 `+F`로 더해지고, 보통 연산량에서는 영향이 작아서 생략하는 경우가 많다.

## 한 줄 요약(Summary)
파라미터는 “저장되는 `W`의 개수”라 `5×5×200×F`만 세고, `30×30`을 곱하면 그건 “같은 `W`를 몇 번 쓰는지”인 연산량(MACs) 계산이다.
