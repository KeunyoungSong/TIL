# Object Detection 가려짐(occlusion) 탐지 정책 설계

> Type: Concept

## 상황/배경(Context)
객체가 “완전히(혹은 일정 비율 이상) 보일 때만” 탐지되길 원하는데, 모델이 일부만 보이는 상황에서도 탐지를 해버리는 문제가 있었다.

---

## 정의(Definition)
가려짐(occlusion) 상황에서의 탐지 억제는 “탐지 모델을 둔감하게 만드는 문제”라기보다, **가시성(visibility) 기준을 별도로 정의하고 그 기준으로 출력(또는 학습)을 통제**하는 문제다.

## 핵심 아이디어(Key Ideas)
- 탐지기는 본질적으로 “부분 단서만으로도 확률이 높으면 박스를 내는” 쪽으로 학습되기 쉽다.
- 요구사항이 “정상적으로 보이는 경우만 OK”라면, **정답 정의(라벨 정책) 또는 후처리 정책**이 먼저 고정돼야 한다.
- 실무적으로는 “탐지(confidence)”와 “가시성(visibility/occlusion)”을 분리해 **2단계 기준**으로 통제하는 방식이 컨트롤이 좋다.

## 예시(Examples)
### 1) 라벨 정책으로 강제하기
- **가려진 객체는 라벨을 달지 않기**: “가려진 건 탐지하면 오답” 신호를 준다.
- **가려진 객체는 ignore로 처리하기**: “배경도 아니고, 탐지 대상으로도 치지 않음”(학습/평가 로스에서 제외).

라벨을 “없애기(negative)”와 “무시하기(ignore)”를 구분하지 않으면, 부분 단서가 배경 패턴으로 학습되는 부작용이 생길 수 있다.

### 2) visibility/occlusion을 별도 예측해서 필터링하기
탐지 모델(또는 파이프라인)에 가시성 점수를 붙이고, 추론 시 정책으로 컷한다.
- 예: `det_conf > t1` AND `visible_ratio > t2`인 것만 출력
- `visible_ratio`는 0~1 회귀, 또는 `occluded / not` 분류로 둘 수 있다.

### 3) 급한 단기 처방: 후처리 규칙(heuristic)
라벨링/모델 변경이 어려우면 “부분일 가능성”을 간접 신호로 막는다.
- 화면 경계에 붙어 있거나 프레임 밖으로 나간 박스는 partial 확률이 높음
- 영상이라면 tracking 기반으로 박스 크기/비율 급변 구간을 억제

## 주의할 점(Caveats)
- “조금만 가려져도 무조건 NO” 같은 정책은 경계가 애매해서 팀 합의가 필요하다. 보통 `visible_ratio >= 0.8`처럼 임계값으로 합의한다.
- 박스만으로 가시성을 정확히 재기 어렵다. 가능하면 마스크(세그멘테이션)나 가시 영역 라벨이 있으면 정책이 안정된다.

## 한 줄 요약(Summary)
가려짐 억제는 “탐지기 성능” 문제가 아니라 “가시성 기준을 별도로 정의·예측·필터링하는 정책 설계” 문제다.

