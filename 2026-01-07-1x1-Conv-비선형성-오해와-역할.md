# `1×1 Conv`의 역할과 “비선형성” 오해

> Type: Concept

## 상황/배경(Context)
GoogleLeNet(Inception)에서 `1×1 Conv`가 자주 등장해서 “비선형성을 강화해준다”라고 이해했는데, 알고 보니 비선형성의 “원인”을 잘못 잡고 있었다. 또한 “`1×1×32 kernel → 6×6×1 output`” 같은 도식은 출력 채널 수 해석에서 착각을 만들기 쉬웠다.

---

## 정의(Definition)
- `1×1 Conv`는 각 spatial 위치 `(i, j)`에서 입력 채널 벡터를 **선형 결합(linear mixing)** 하는 convolution이다.
- 비선형성(non-linearity)은 `Conv`가 아니라 **activation(ReLU 등)** 에서 생긴다.

## 핵심 아이디어(Key Ideas)
- `1×1 Conv` 자체는 선형이다. “비선형성을 만든다”는 표현은 틀리다.
- 다만 `1×1 Conv`를 사이에 끼워 넣으면, (보통 뒤에 activation을 붙이므로) **비선형 변환을 넣을 ‘층’이 하나 더 생긴다**는 의미에서 “비선형성을 더 활용한다”는 말이 나올 수 있다.
- 출력 채널 수는 커널 크기(1×1)가 아니라 **필터 개수(filters)** 로 결정된다.
  - 입력이 `(6, 6, 32)`일 때 `filters=1`이면 출력 `(6, 6, 1)`, `filters=32`면 출력 `(6, 6, 32)`다.
- Inception에서의 `1×1 Conv`는 주로 **채널 축소(bottleneck)** 로 연산량을 줄이고, 채널을 섞어 표현을 재구성한 뒤 큰 커널(`3×3`, `5×5`)을 적용하기 위한 장치다.

## 예시(Examples)
### 1) “한 위치에서” `1×1 Conv`가 하는 계산
입력이 `(6, 6, 32)`라면, 어떤 위치 `(i, j)`의 입력은 길이 32 벡터다.

- 입력(예시): `x ∈ R^32`
- 필터 1개는 가중치 `w ∈ R^32`와 bias `b`를 가진다.

그 위치의 출력 스칼라(필터 1개 기준):
`y = w^T x + b`

필터를 `F`개 쓰면, 같은 위치에서 `F`개의 스칼라가 나오므로 출력 벡터는 `R^F`가 된다.

### 2) 슬라이드의 “`1×1×32 kernel → 6×6×1 output`” 해석
이 표기는 보통 아래 의미다.
- 커널의 depth(입력 채널)는 32 → `w ∈ R^32`
- **필터가 1개**라서 출력 채널이 1 → `(6, 6, 1)`

즉 “`1×1이라서 채널이 1이 된다`”가 아니라, “**필터를 1개만 써서** 채널이 1이 된다”가 정확하다.

### 3) 왜 Inception에서 `1×1 Conv`를 끼우나(연산량 관점)
입력 채널이 32일 때:
- `3×3 Conv`의 한 위치 연산량: `3×3×32 = 288` (곱셈 기준)
- `1×1 Conv`의 한 위치 연산량: `1×1×32 = 32`

그래서 `1×1`로 채널을 먼저 줄인 뒤(예: 32→8), 큰 커널을 적용하면 비용을 크게 줄일 수 있다.

## 주의할 점(Caveats)
- “비선형성이 늘어난다”를 정확히 말하면:
  - `1×1 Conv`가 비선형을 만들진 않지만,
  - `1×1 Conv + activation` 같은 블록을 추가하면 네트워크가 더 많은 비선형 단계를 갖게 된다.
- `1×1 Conv`는 공간(H, W)을 보지 않으므로 공간적 패턴을 직접 잡는 건 `3×3`, `5×5` 같은 커널의 역할이다.

## 한 줄 요약(Summary)
`1×1 Conv`는 채널을 섞는 선형 변환이고, 비선형성은 activation이 만든다; Inception에서 `1×1`은 주로 bottleneck(연산량 절감)과 채널 재구성(표현력) 때문에 쓰인다.

---

## 용어(Glossary)
- **filters**: convolution 필터(=출력 채널) 개수.
- **bottleneck**: 연산량을 줄이기 위해 채널을 먼저 축소하는 설계.
