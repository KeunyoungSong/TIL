# Weight Regularization(L1/L2) 패널티 항과 L2 norm 표기 읽는법

> Type: Concept

## 상황/배경(Context)
가중치 규제가 “loss를 무디게 만든다”는 느낌으로 이해하면 왜 오버피팅이 줄어드는지/수식이 어떻게 바뀌는지/`||W||_2^2` 표기가 왜 2가 두 번 나오는지에서 막히기 쉬웠다.

---

## 정의(Definition)
Weight Regularization은 학습 목표를 `데이터 오차`만 최소화하는 문제에서, `데이터 오차 + 가중치 패널티`를 최소화하는 문제로 바꿔 “큰 가중치를 써서 외우는 해”를 불리하게 만드는 방법이다.

## 핵심 아이디어(Key Ideas)
- 규제는 loss 값을 “속이는” 게 아니라, **좋은 해의 기준(목적함수)**을 바꾼다.
- L2는 큰 가중치에 더 큰 벌점을 주어 가중치를 0 쪽으로 끌어당기는 압력을 만든다(“weight decay”로 불리는 이유).
- 보통 모든 파라미터가 아니라, 규제를 걸어둔 레이어의 `kernel weight`들에 대해 패널티가 합산된다(관례적으로 `bias`는 제외하는 경우가 많음).

## 기호 정리(Symbols)
- `W`: 모델의 가중치(보통 레이어들의 kernel weight들을 모아 생각)
- `L_data(W)`: 데이터에서의 손실(예: CrossEntropy, MSE)
- `λ`: 규제 강도(얼마나 가중치 큰 걸 싫어할지)
- `||W||_2`: L2 norm(유클리드 길이)

## 원리/수식(Principle/Math)
### 1) 목표함수(기준)가 어떻게 바뀌나
규제 없음:

$$
\min_W \; L_{data}(W)
$$

L2 규제:

$$
\min_W \; \Big(L_{data}(W) + \lambda \cdot ||W||_2^2\Big)
$$

### 2) `||W||_2^2`에서 2가 두 번 나오는 이유
- 아래첨자 `2`: “L2 방식으로 길이(크기)를 재겠다”(norm의 **종류**)
- 위첨자 `2`: “그 결과를 제곱해서 쓰겠다”(연산)

그리고 실제 계산값은 다음과 같다.

$$
||W||_2 = \sqrt{\sum_j w_j^2}
$$

$$
||W||_2^2 = \sum_j w_j^2
$$

즉, 딥러닝에서 흔히 말하는 “L2 패널티”는 **가중치 제곱합**이다.

### 3) 학습(gradient)에서 어떤 압력이 생기나
패널티 항은 미분하면 `w`에 비례하는 항이 추가된다.

$$
\frac{\partial}{\partial w_j}\Big(\lambda w_j^2\Big)=2\lambda w_j
$$

그래서 가중치가 커질수록 더 강하게 0 방향으로 끌리는 효과가 난다.

## 예시(Examples)
### L1 vs L2 패널티 계산
가중치가 `w=[0.1, -0.2, 0.5]`이면:
- L2 패널티: $0.1^2+(-0.2)^2+0.5^2=0.30$
- L1 패널티: $|0.1|+|-0.2|+|0.5|=0.8$

### Keras에서 “레이어별로” 패널티가 합쳐지는 방식
```python
Dense(128, kernel_regularizer=tf.keras.regularizers.l2(1e-3))
Dense(10,  kernel_regularizer=tf.keras.regularizers.l2(1e-3))
```
위처럼 지정하면, 각 레이어의 kernel weight에 대해 계산된 패널티들이 최종 loss에 **더해져서** 학습된다.

## 주의할 점(Caveats)
- “훈련 loss가 낮아지는 것”과 “일반화가 좋아지는 것”은 별개라서, 규제는 둘 사이의 트레이드오프를 조절하는 장치다(`λ` 튜닝 필요).
- 프레임워크/옵티마이저에 따라 “L2 regularization”과 “weight decay(AdamW 등)”가 구현적으로는 다르게 적용될 수 있다(헷갈리면 목적함수에 어떤 항이 더해지는지부터 확인).

## 한 줄 요약(Summary)
가중치 규제는 `L_data(W)`에 `λ·||W||_2^2(=가중치 제곱합)` 같은 벌점 항을 더해 “큰 가중치를 쓰는 해”를 점수상 불리하게 만드는, 목적함수(기준) 변경이다.
