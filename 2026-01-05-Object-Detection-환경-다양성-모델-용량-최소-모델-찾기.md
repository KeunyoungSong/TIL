# Object Detection 환경 다양성과 모델 용량, 최소 모델 찾기

> Type: Concept

## 상황/배경(Context)
단일 클래스 탐지지만 환경(배경/조명/시점/스케일/가림)이 매우 다양할 때, 데이터(예: 10만 장)를 늘리면 모든 환경에서 자동으로 잘 될지, 그리고 “모델 용량(capacity)”을 어떻게 판단할지 정리했다.

---

## 정의(Definition)
모델 용량(capacity)은 단순 “학습 가능한 이미지 수”가 아니라, **환경 다양성에 필요한 표현 복잡도를 동시에 수용할 수 있는 정도**를 말한다. 데이터만 늘린다고 모든 환경에서 성능이 보장되지는 않는다.

## 핵심 아이디어(Key Ideas)
- 데이터는 많아도 분포가 불균형이면, 모델은 “다수 환경 평균 성능”을 우선 최적화해서 소수 환경이 희생될 수 있다.
- 용량 판단은 감이 아니라 **학습 곡선(train vs val), 환경별 분해 성능, 데이터 증가 대비 성능 기울기**로 한다.
- 전체 mAP 하나만 보면 착시가 크다. 환경 태그(주간/야간, 실내/실외, 근/원거리 등)로 쪼개서 본다.

## 예시(Examples)
### 1) 용량 부족/과적합을 구분하는 신호
- **용량 부족**: train loss 자체가 일정 수준 아래로 잘 안 내려감(데이터 늘려도 비슷), 어려운 환경 성능이 특히 막힘
- **과적합/정의 문제**: train은 좋은데 val이 안 따라오거나 특정 환경만 붕괴
- **수렴(flattening)**: 데이터 2배를 넣어도 성능 개선 폭이 급격히 줄어듦 → 같은 구조로는 상한에 가까움

### 2) 최소 “안정 용량”을 빠르게 찾는 실험 설계(Reverse ablation)
1) 대표 환경 일부(예: 20~30%)로 **상대적으로 큰 모델**로 빠르게 ceiling을 본다(목표는 배포가 아니라 “문제 난이도 측정”).
2) validation을 환경별로 쪼개서 **환경별 ceiling/바닥**을 확인한다.
3) 모델만 줄인다: 큰 모델 → 작은 모델로 단계적으로 내려오며, **어느 단계에서 특정 환경 성능이 급락하는지** 찾는다.
4) 그 급락 직전 모델이 보통 “이 문제의 최소 안정 용량”이 된다.
5) 같은 후보에서 데이터 비율(100/50/25%)도 줄여 “모델 부족 vs 데이터 부족”을 분리한다.

## 주의할 점(Caveats)
- “환경 1000가지”는 난이도가 아니라 분포 문제일 수 있다(환경별 샘플 수/품질/라벨 일관성 확인이 우선).
- 특정 환경이 큰 모델에서도 낮게 고정되면, 모델 문제가 아니라 **문제 정의/시각 정보 부족/라벨 정책(가려짐 포함)** 문제일 수 있다.

## 한 줄 요약(Summary)
환경이 다양할수록 데이터 개수보다 “환경별 균형·평가 분해·용량 붕괴 지점 탐색(큰 모델로 ceiling → 줄여가며 최소 안정 용량 찾기)”이 더 중요하다.

